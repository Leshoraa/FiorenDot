# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hypridle 
# Original config submitted by https://github.com/SherLock707                                                       
                                                      
#$iDIR="$HOME/.config/swaync/images/ja.png"

general {
    lock_cmd = pidof hyprlock || hyprlock # runs hyprlock if it is not already running (this is always run when "loginctl lock-session" is called) 
    unlock_cmd = pkill -x hyprlock || true # kills any remaining hyprlock instance when unlocking
    before_sleep_cmd = loginctl lock-session    # ensures that the session is locked before going to sleep
    after_sleep_cmd = hyprctl dispatch dpms on && systemctl --user restart wireplumber # turn on screen & auto-recover audio stack after sleep
    ignore_dbus_inhibit = false             # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
}

# Turn off screen faster if session is already locked
listener {
    timeout = 30                            # 30 seconds
    on-timeout = pidof hyprlock && hyprctl dispatch dpms off # turns off the screen if hyprlock is active
    on-resume = pidof hyprlock && hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
}

# Dim screen to save battery
listener {
    timeout = 150                                            # 2.5 min
    on-timeout = brightnessctl -s set 10%                    # set monitor backlight to 10%
    on-resume = brightnessctl -r                             # monitor backlight restore
}

# Warn
listener {
    timeout = 270                     # 4.5 min
    on-timeout = notify-send "Idle Warning" "Sistem akan segera dikunci!"
}

# Screenlock
listener {
    timeout = 300                     # 5 min
    on-timeout = loginctl lock-session # command to run when timeout has passed
}

# Turn off screen 
listener {
    timeout = 330                            # 5.5 min
    on-timeout = hyprctl dispatch dpms off  # command to run when timeout has passed
    on-resume = hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
}

# Suspend
listener {
    timeout = 1200                            # 20 min
    on-timeout = systemctl suspend            # suspend when timeout has passed
}
